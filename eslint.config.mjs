import { defineConfig, globalIgnores } from "eslint/config";
import nextVitals from "eslint-config-next/core-web-vitals";
import nextTs from "eslint-config-next/typescript";

const eslintConfig = defineConfig([
  ...nextVitals,
  ...nextTs,
  {
    rules: {
      // La app hidrata su estado desde localStorage y hace fetch del clima
      // dentro de effects (patrón seguro para SSR: se renderiza el estado
      // inicial y luego se actualiza en el cliente). No usamos una librería
      // de datos, así que permitimos setState dentro de useEffect.
      "react-hooks/set-state-in-effect": "off",
    },
  },
  globalIgnores([
    ".next/**",
    "out/**",
    "build/**",
    "next-env.d.ts",
    "src/generated/**",
  ]),
]);

export default eslintConfig;
